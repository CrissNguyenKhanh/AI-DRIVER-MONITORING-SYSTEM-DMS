Flask==3.0.0
Flask-CORS==4.0.0

# WebSocket / SocketIO
flask-socketio==5.3.6
python-socketio==5.10.0
python-engineio==4.8.0
eventlet==0.35.1

# WSGI server (Render)
gunicorn==21.2.0

# Database — chỉ cần psycopg2 trên Render (PostgreSQL)
psycopg2-binary==2.9.9

# JWT & Security
Flask-JWT-Extended==4.5.3
Werkzeug==3.0.1

# Machine Learning
scikit-learn==1.7.2
numpy==1.26.4
joblib==1.4.2
threadpoolctl==3.5.0

# Image processing — headless
Pillow==10.0.1
opencv-python-headless==4.8.1.78
mediapipe==0.10.9

# YOLO phone detection dùng ONNX (không cần PyTorch, ~80MB)
onnxruntime-cpu==1.18.0
