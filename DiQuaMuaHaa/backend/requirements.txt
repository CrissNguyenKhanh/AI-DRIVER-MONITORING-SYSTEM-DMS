Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1

# WebSocket / SocketIO
flask-socketio==5.3.6
python-socketio==5.10.0
python-engineio==4.8.0
eventlet==0.35.1

# WSGI server (Render)
gunicorn==21.2.0

# Database
PyMySQL==1.1.0
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9

# JWT & Security
Flask-JWT-Extended==4.5.3
Werkzeug==3.0.1

# Machine Learning (1.7.x khớp file .pkl đã train; 1.3.x gây InconsistentVersionWarning / lỗi unpickle)
scikit-learn==1.7.2
numpy==1.26.4
scipy==1.11.4
joblib==1.4.2
threadpoolctl==3.5.0

# Image processing — headless (không cần GUI/display, dùng trên server)
Pillow==10.0.1
opencv-python-headless==4.8.1.78
mediapipe==0.10.9

# YOLO phone detection dùng ONNX (không cần PyTorch, tiết kiệm ~200MB RAM)
onnxruntime-cpu==1.19.2

# Optional
pandas==2.1.3
